import type { MockFlightEntry } from '../fixtures/mockFlights';

interface Props {
  flights: MockFlightEntry[];
}

// Stub — implementation pending. Tests will fail against this placeholder.
export default function MockFlightBoard(_props: Props): null {
  return null;
}
