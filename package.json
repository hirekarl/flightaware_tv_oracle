{
  "name": "flightaware-tv-oracle",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Root workspace: Husky git hooks + commitlint only",
  "scripts": {
    "prepare": "husky"
  },
  "devDependencies": {
    "@commitlint/cli": "^19.0.0",
    "@commitlint/config-conventional": "^19.0.0",
    "husky": "^9.0.0"
  }
}
